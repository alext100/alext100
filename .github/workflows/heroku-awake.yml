name: Heroku Awake

on:
  schedule:
    - cron: "*/35 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: mikeesto/heroku-awake@1.0.0
        with:
          URL: "https://git.heroku.com/fast-savannah-95653.git"
